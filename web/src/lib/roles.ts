// The console reads the daemon's roster straight from the source of truth —
// the sibling src/roles.ts (pure data, no runtime imports).
export { ROLES, type RoleKey, type RoleDef } from "../../../src/roles";
