node_modules/
.env
.env.*
!.env.example
# Root-anchored: these are the daemon's runtime dirs. Unanchored `outbox/`
# once swallowed web/src/app/outbox/ — the console page never reached GitHub.
/outbox/
/reports/
*.log
.DS_Store
